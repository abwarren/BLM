"""
BLM V3 — High-Frequency Collector Package.

Extends the V1 Playwright scraping approach with higher-frequency capture
(250ms configurable), pace calculation, movement delta tracking, and direct
writes to ``blm_historical.db``.
"""

__version__ = "3.0.0"
